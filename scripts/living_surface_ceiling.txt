# Maximum lines of LIVING prose. A RATCHET: it may only come DOWN.
#
# Every line in a LIVING doc is a claim that must match the code, so every
# line is a thing that can become false. Fifteen nightly runs found real
# drift and none found zero, because this number never moved. Cycle 16 was
# the first to shrink it (8,331 -> 8,317) under the deletion contract.
#
# Lower it when prose is removed. Raise it ONLY for content that is
# genuinely WHY -- a product rule, a reason the code cannot hold -- and say
# in the commit what was added. A restatement of code is never a reason to
# raise it: delete the restatement or point at the symbol instead.
#
# Set 2026-08-25 at the then-current main: 8331.
# Lowered 2026-08-25 to 8317 after cycle 16 (#427) deleted 14 lines --
# a ceiling left above the real number is slack the haystack grows into.
# Lowered again 2026-08-25 to 8313 after cycle 17 (#431) deleted 4 more.
# Raised 2026-08-25 to 8351. NOT slack: main gained two new LIVING docs
# (delivery plans, now stamped) and cycle PRs #427/#431/#433 landed while
# this branch was open. The rule is that a raise must say what was added,
# and this is what was added.
# 8353 (2026-08-25): +2 blank lines from moving two stamps BELOW their YAML
# front matter — placing them above stopped the front matter parsing, which
# silently unloads a skill. Structural, not new prose.
# 8359 (2026-08-25): +6 for the 'API Routes' heading and the two lines of
# prose introducing it. The 65 ROUTES themselves cost zero -- they are a
# generated block. Six lines of WHY bought 65 facts that can never drift,
# and route lies are the most expensive class in this repo: a wrong endpoint
# reads like a contract and 404s whoever trusts it.
# 8738 (2026-08-25): +379 for docs/decisions/ — six ADRs and an index.
#
# This is the raise this escape hatch was built for, and the clearest test
# of whether the rule is understood. An ADR records WHY: what was decided,
# what problem it solved, what was rejected, what it costs. None of that is
# derivable from code -- the code shows the choice and never the alternatives
# -- so no generator can produce it and no truth-checker can verify it.
#
# It is therefore the one kind of prose that CANNOT drift into a lie about
# the code, because it makes no claim about what the code currently does.
# Deleting it to make a number go down would be the metric eating the thing
# it exists to protect.
#
# The rule stands for everything else: restatement of code is never a reason
# to raise this ceiling.
# 8697 (2026-08-25): lowered after the CLAUDE.md diet moved the 31 hard rules
# into a skill. Left at 8738 it would have been 41 lines of SLACK, and the
# mutation drill proved it: a planted 40-line regression went UNREPORTED.
#
# That is the rule this file already states, broken by the commit that shrank
# the estate. A ceiling above the real number is not caution, it is a ratchet
# that has stopped ratcheting. Lower it every time the floor drops.
# 8764 (2026-08-25): +67 from the CLAUDE.md diet, and the reason matters.
# The 31 hard rules moved from CLAUDE.md into a SKILL. Both files are LIVING,
# so no prose left the estate -- it relocated, and the skill's own header
# explaining the move added lines.
#
# This is a LIMITATION of this metric, recorded rather than hidden: it counts
# prose VOLUME, not CONTEXT COST. Moving 990 words out of a file that loads
# before every single session into one that loads on demand is a large win,
# and this number scores it as slightly worse. Volume and context cost are
# different things and only one of them has a meter.
# (this branch, 2026-08-25): +42 net. Nine docs DELETED (-3,123 lines of scaffolding,
# already excluded from this count as FROZEN, so they never showed here) and
# three rewritten. The +42 is the rewrites: docs/archive/README.md now explains
# that it describes an EMPTY directory while the real archive is docs/_archive/
# — an accident that had every archiving run putting files in the wrong place —
# and DOC-MAINTENANCE.md records which docs may be deleted and which may not,
# with the reason. Both are WHY, not restatement of code.
#
# MERGE NOTE (2026-08-26): this branch keeps main's 8764 and does not move it.
# The nine documents it deletes were FROZEN, so they never counted here, and the
# ~42 lines of WHY it adds sit on top of an overshoot that already exists on
# main -- `doc_sync_check` measures 10,291 against this ceiling on origin/main
# itself, before this branch touches anything. Raising a ratchet to cover a gap
# somebody else opened is how a ratchet stops meaning anything; that gap is its
# own piece of work, and it is not this PR.
# 5133 (2026-09-05, slice 5 #483): lowered by 3,631. The sourcing era's docs
# (pillars, runbook, glossary, catalog reports) were archived FROZEN and
# README.md was rewritten from 469 lines to 140. The mutation drill proved
# the old number had become slack: a planted 40-line regression in STATUS.md
# went unreported under 8764. A ceiling above the real number is a ratchet
# that has stopped ratcheting.
# 4951 (2026-09-05, slice 5 review): the review fix batch trimmed stale skill
# and STATUS lines after 5133 was measured, leaving 182 lines of slack — the
# same shape the previous entry warns about. Re-measured, not estimated.
4951
