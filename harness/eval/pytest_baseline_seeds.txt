# Pinned seeds proving a deterministic 3-run green baseline (post-Step-0 pre-flight).
# Regenerate after substantive scoring / dedup / embedding changes.
#
# Generated on 2026-04-24 — Step-0 pre-flight.
# Full suite: `cd backend && python -m pytest -q -p no:randomly --tb=no`
# Expected: 600+ passed / 0 failed / 3 skipped.
#
# Random-order runs (proving independence):
#   seed=1  — python -m pytest -q -p randomly --randomly-seed=1  → green
#   seed=7  — python -m pytest -q -p randomly --randomly-seed=7  → green
#   seed=42 — python -m pytest -q -p randomly --randomly-seed=42 → green
